import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'teamProject_2024_04',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
